[project]
name = "pyromod"
version = "3.6"
description = "A monkeypatcher add-on for Pyrogram that does conversation handling"
authors = [
    { name = "Pauxis", email = "pauxiscezar@gmail.com" },
    { name = "Meybj", email = "8ddbhlymd@mozmail.com" }
]
dependencies = [
    "kurigram>=2.2.12",
]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true
